/**
 * main - Entry point
 *
 * Description: prints numbers
 *
 * Return: 0 Always (Success)
*/
#include <stdio.h>
int main(void)
{
	for (int i=0; i<10; i++)
	{
		printf("%d\n", i);
	}
	return (0);
}
